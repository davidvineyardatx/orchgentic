# Orchgentic v0.7.6-alpha — Create CLI DX Hotfix

Adds the missing create command group:

```bash
orch create agent Bob
orch create team ContentTeam
orch create trigger DailyReport
```

This fixes the documentation/runtime mismatch discovered after the public developer-alpha release.
